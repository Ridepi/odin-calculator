const add = function(a, b) {
    result = a + b;
    return result
}

const min = function(a,b) {
    result = a - b;
    return result
}

const div = function(a, b) {
    result = a/b;
    return result
}

const mult = function(a, b) {
    result = a*b;
    return result
}
